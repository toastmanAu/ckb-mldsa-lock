//! Integration test harness for `mldsa-lock-v2`.
//!
//! This crate has no library code of its own — see `tests/` for the actual
//! test cases driving the compiled v2 contract binaries through
//! `ckb-testtool`'s VM simulator.
